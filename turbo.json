{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "type-check": {},
    "lint": {},
    "build": {
      "dependsOn": ["^type-check"]
    },
    "deploy": {
      "dependsOn": ["^build"],
      "env": ["TAILOR_TOKEN", "WORKSPACE_ID"]
    }
  }
}
